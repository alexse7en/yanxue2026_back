<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.yw.ps.dal.mysql.YwAuthLevelMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->



    <insert id="beginAuth"  parameterType="map" useGeneratedKeys="true" keyProperty="param.id" keyColumn="id">
        INSERT INTO yw_auth_level (
            name, member_id,   level_id,score
        )
        SELECT
            e.name as name,

            #{param.memberId} AS member_id,
            e.id AS level_id,
            e.score
        FROM yw_level e
        WHERE
            e.id = #{param.levelId}
    </insert>

    <insert id="beginAuthCondition">
        INSERT INTO yw_auth_condition (
            name,   level_id, member_id, auth_id, condi_id
        )
        SELECT
            e.name as name,
            #{levelId} AS level_id,
            #{memberId} AS member_id,
            #{authId} AS auth_id,
            e.id AS condi_id
        FROM yw_level_condition e
        WHERE
            e.level_id = #{levelId}

    </insert>

    <insert id="beginAuthDetail">
        INSERT INTO yw_auth_detail (
            name, member_id,   level_id, auth_id, detail_id
        )
        SELECT
            e.name as name,
            #{levelId} AS level_id,
            #{memberId} AS member_id,
            #{authId} AS auth_id,
            e.id AS detail_id
        FROM yw_level_group_detail e

    </insert>
    <select id="SelectAuthCondition" resultType="cn.iocoder.yudao.module.yw.ps.vo.YwAuthConditionMemberVo">
        select a.*, b.introduce, b.iz_force, b.input_type  from yw_auth_condition a
        inner join  yw_level_condition b on a.condi_id=b.id
        where a.auth_id=#{authId}

    </select>
    <select id="SelectAuthDetail" resultType="cn.iocoder.yudao.module.yw.ps.vo.YwAuthDetailMemberVo">

        select a.*, b.score, b.exclude_level_id,
               c.name as norm_name, c.introduce as norm_introduce, c.max_score as norm_score, c.input_type, c.id as norm_id,
            d.name as group_name, d.introduce as group_introduce, d.max_score as group_score, d.id as group_id
        from yw_auth_detail a
        inner join  yw_level_group_detail b on a.detail_id=b.id
        inner join  yw_level_group_norm c on b.norm_id=c.id
        inner join  yw_level_group d on c.group_id=d.id
        where a.auth_id=#{authId}
        order by group_id,norm_id,detail_id
    </select>

    <select id="SelectAuthConditionForTeacherComment" resultType="cn.iocoder.yudao.module.yw.ps.vo.YwAuthConditionMemberVo">
        select a.*, b.introduce, b.iz_force, b.input_type, d.status as comment_status, d.bz  ,d.id as comment_id
        from yw_auth_condition a
               inner join  yw_level_condition b on a.condi_id=b.id
               inner join yw_auth_level c on a.auth_id=c.id and CONCAT(',', c.teacher, ',') LIKE CONCAT('%,', #{memberId}, ',%')
               left join yw_auth_comment d on d.auth_id=c.id and d.comment_type='CONDI' and   d.teacher_id =#{memberId} and d.detail_id=a.id
        where a.auth_id=#{authId} and a.iz_selected='1'
        order by b.id

    </select>
    <select id="SelectAuthDetailForTeacherComment" resultType="cn.iocoder.yudao.module.yw.ps.vo.YwAuthDetailMemberVo">

        select a.*, b.score, b.exclude_level_id,
            c.name as norm_name, c.introduce as norm_introduce, c.max_score as norm_score, c.input_type, c.id as norm_id,
               d.name as group_name, d.introduce as group_introduce, d.max_score as group_score, d.id as group_id,
               f.status as comment_status, f.bz ,f.id as comment_id
        from yw_auth_detail a
                 inner join  yw_level_group_detail b on a.detail_id=b.id
                 inner join  yw_level_group_norm c on b.norm_id=c.id
                 inner join  yw_level_group d on c.group_id=d.id
                 inner join yw_auth_level e on a.auth_id=e.id and  CONCAT(',', e.teacher, ',') LIKE CONCAT('%,', #{memberId}, ',%')
                 left join yw_auth_comment f on f.auth_id=e.id and f.comment_type='DETAIL' and f.detail_id=a.id and f.teacher_id=#{memberId}
        where a.auth_id=#{authId} and a.iz_selected='1'
        order by group_id,norm_id,detail_id
    </select>
</mapper>